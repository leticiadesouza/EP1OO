�Atenção: Este é um exercício de programação que visa a aplicação de alguns conceito aprendidos durante a disciplina de orientação a objetos. Recomenda-se o trabalho contínuo em tal exercício, pois o mesmo dificilmente será implementado em um ou dois dias com a devida qualidade exigida. Este exercício busca avaliar os seguintes conceitos: Criação de classes e objetos. Utilização de atributos, métodos e construtores. Utilização de herança. Para o melhor aproveitamento do aluno, recomenda-se que o mesmo busque várias referências externas para auxiliar na resolução do exercício. O objetivo deste trabalho é implementar programa capaz de ler arquivos de imagens nos formatos PGM e PPM e extrair informações embutidas nestas imagens através da técnica da esteganografia. Fundamentação Teórica Esteganografia Para quem não está familiarizado com segurança de dados e pessoas nos meios eletrônicos a estenografia pode parecer um termo estranho. Mesmo na cultura pop, em séries e filmes com essa temática essa palavra e tudo aquilo que ela envolve é deixado um pouco de lado. O que é esteganografia afinal? Esteganografia é uma palavra que vem do grego e significa “escrita oculta”. Trata-se do estudo de técnicas que permitam esconder informações dentro de outros arquivos, sejam imagens, músicas, vídeos ou mesmo textos. Por exemplo, é possível ocultar mensagens dentro de imagens de uma forma que qualquer pessoa que não a pessoa para a qual a mensagem está destinada, ou sem o conhecimento necessário, sequer desconfie que exista algo além da imagem que está sendo mostrada. A esteganografia está presente em nossa história há séculos. Técnicas como as tintas “invisíveis” e os micropontos são consideradas um tipo de esteganografia. Apesar da esteganografia ser um dos vários ramos dentro da criptografia, ela não pode ser detectada, ao contrário de arquivos cifrados. E é justamente por isso que essas técnicas para ocultar informações dentro de arquivos podem ter infinitas aplicações. Qual a utilidade? Como dito antes, essa técnica pode ter infinitas aplicações, a mais comum para nós é a inserção de mecanismos de verificação de direitos autorais em fotografias, músicas e vídeos. Outra aplicação é divulgação de mensagens para destinatários específicos através da comunicação aberta. Também pode ser utilizada de maneira prejudicial para a distribuição de vírus e malware escondidos em um arquivo comum. Exercício Objetivos Esse exercício de programação tem dois objetivos principais que devem ser alcançados pelo aluno, sendo eles: Interpretação de mensagens escondidas em imagens por meio da técnica da esteganografia; Aplicação dos conceitos de Orientação a objetos. Para a elaboração desse E.P será levado em consideração os seguintes conceitos de O.O.: Classes; Atributos; Métodos; Construtores; Encapsulamento; Herança. Dessa forma, o aluno deverá tornar mensagens escondidas em imagens evidentes utilizando o paradigma Orientado a Objetos. Fase Zero: Preparação Visando manter seu projeto organizado, crie a seguinte estrutura de pastas: bin: Pasta onde será mantido o binário.inc: Pasta que manterá os headers.src: Pasta que manterá as implementações dos hearders.obj: Pasta que receberá os arquivos objetos. doc: Pasta onde conterá qualquer informação extra de documentação. Ativar o controle de versão Git, na pasta do projeto. Inicialize o controle de versão: git init Adicione a estrutura de pastas e/ou arquivos já existentes: git add . Faça o commit inicial do projeto: git commit -m Commit Inicial Criar o repositório Git remoto no Gitlab ou Github. Por fim, crie os seguintes arquivos: Makefile: Crie o arquivo Makefile que compilará o seu código e salvará o arquivo compilado na pasta bin. README.md: Explique de forma ORGANIZADA e direta como compilar e executar o seu programa, inclusive o caminho das imagens. Primeira Fase: Trabalhando com a imagem A primeira parte do exercício consiste em criar um programa em C++ que manipule imagens dos tipos PGM e PPM. O seu programa deve realizar no mínimo as seguintes tarefas:Ler arquivos de imagens nos formatos PGM e PPM, a partir de seu caminho em disco. Criar um método que permita salvar a imagem correspondente à solução do problema com local e nome desejado. Imprimir a mensagem na tela e/ou em arquivo caso esteja escondida em arquivo PGM.Espera-se a implementação de pelo menos uma classe contendo os atributos para manter os dados da imagem e métodos para manipular tais dados.Segunda Fase: Decifrando a mensagemApós ler e armazenar os dados referentes às imagens, crie uma maneira de aplicar as seguintes transformações:Aplicação do filtro R Aplicação do filtro G Aplicação do filtro B Identificar mensagem escondida nos bytes da imagem.Lembre-se que o seu objetivo é utilizar os conceitos de Orientação a Objetos, logo recomenda-se uma reflecção buscando abstrair a melhor forma de unir os dois tipos de solução.Espera-se pelo menos uma classe geral e classes filhas que implementem dos filtros RGB e identificação do texto.DICA: Crie uma classe mãe decifragem e as classes filhas que decifram mensagem escondida na cor da imagem PGM outra para PPM.Algoritmos Como a implementação dos algoritmos não é o foco deste trabalho, segue o diagrama e os trechos de código que serão necessários. Apesar do código estar sendo fornecido, vale observar que é trabalho do estudante entender e adaptar os trechos de código.Leitura da imagem Algoritmo geral para leitura de imagens PGM e PPM. Decifrar imagem PGMO início da mensagem pode ser aleatório dentro da imagem. Porém, para facilitar a implementação, a posição inicial será informada dentro do arquivo da imagem após o valor da escala de cinza. Além disso, as mensagens tem tamanho variável e sempre terminam com o caractere. Para ler a mensagem escondida na imagem temos o seguinte algoritmo: loop_enquanto( posicao_de_leitura < tamanho_da_imagem ) {Posicionar o ponteiro de leitura na posição inicial da mensagem Ler o LSB (Least Significant Bit) - último bit de oito bytes seguidos para montar um único caractere. } Se (charactere_do_byte == marca_mensagem){ charactere_do_byte = 0Enquanto (charactere_do_byte != marca_mensagem && NÃO_CHEGOU_NO_FIM_DA_IMAGEM) { Para (i = 0; i < 8; ++i) { charactere_auxiliar = BITE_CHEIO & lista_pixels[iterador] charactere_do_byte = charactere_do_byte_deslocado_1_bit_para_esquerda charactere_do_byte = (charactere_do_byte & 11111110) OU (charactere_auxiliar & 00000001) } Se (charactere_do_byte != marca_mensagem) cout << charactere_do_byte else cout << endl } } Se(charactere_do_byte == marca_mensagem) break Decifrar imagem PPM Para (i = 0; i < imagem -> largura; i++) { Para (j = 0; j < imagem -> altura; j++) { Se (escolha_do_filtro == cor) { nova_imagem[i][j] -> cor = imagem[i][j] -> cor nova_imagem[i][j] -> outra_cor = 0 } Senão Se (escolha_do_filtro == outra_cor)  { nova_imagem[i][j] -> outra_cor = imagem[i][j] -> outra_cor nova_imagem[i][j] -> cor = 0 } Orientação a Objetos A Orientação à Objetos é o paradigma de programação que se fundamenta no reuso de objetos, abstraindo-os do mundo real, permitindo assim uma melhor manutenibilidade e compreensão do código.Neste trabalho, assim como durante toda a disciplina de OO, será observado e avaliado a utilização dos conceitos de OO. Classes Em OO, classes são utilizadas para criar tipos definidos pelo usuário, permitindo a criação de instâncias dessas classes, denominadas objetos. Os objetos são criados a partir do método construtor da classe, sendo também destruídos quando necessário.As classes são muito importantes para abstrair objetos do mundo real, definindo suas características (atributos) e comportamentos (métodos). Em C++ há uma divisão entre declaração e definição das classes. Sendo a utilização dessa prática um dos critérios avaliados neste trabalho, assim como a separação das classes em arquivos distintos e a utilização de uma main() enxuta. Declaração - Headers (.hpp) Os headers contém a declaração das funções e atributosda classe que deverão obrigatoriamente ser implementados, incluindo os construtores. Constantes devem ser declaradas e definidas nos headers. Definição - (.cpp) Nesses arquivos deverá ser definido o comportamento das funções declaradas no header bem como possíveis valores iniciais para os atributos. Herança Um importante conceito utilizado na Orientação à Objetos é a herança. A herança permite que uma classe herde características comuns da classe herdada (classe base). Isso permite que as classes derivadas mantenham características e comportamentos em comum com a classe base e modifiquem comportamentos e características próprias. Dessa maneira, será avaliada a capacidade do aluno em descobrir a melhor maneira de organizar suas classes e utilizar o conceito de hierarquia na construção das mesmas. Por exemplo, os diferentes tipos de protocolos podem utilizar o conceito de hierarquia. Polimorfismo Polimorfismo é a capacidade que os objetos possuem de se comportar de maneira diferente de acordo com o contexto em que estão inseridos, alterando seus comportamentos padrão. Um exemplo disso é mostrado na imagem acima em que as classes derivadas sobrescrevem o método area() herdado da classe base. Assim, o método area() é adaptado pelas classes derivadas. No entanto, essa é uma das formas mais básicas de polimorfismo. Outros exemplos incluem: funções virtuais; classe abstrata; classe base virtual etc. É importante que o aluno consiga visualizar a possibilidade da utilização do polimorfismo neste trabalho e utilizá-lo. Interface - Classe abstrata A classe abstrata é uma classe base que tem métodos definidos como funções virtuais puras. Com isso, a classe abstrata não pode ser utilizada para instanciar objetos, servindo apenas de base para as classes derivadas. Além disso, as classes derivadas são obrigadas aJq��2�<��y�+((���F=,�A�'q����h����x������o
�gyLh�{��@$�fj�^�`�B%Y�mi�TB0�!� ��)��8�WZ�sDv�#